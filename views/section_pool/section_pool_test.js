(function (angular) {
	"use strict";

	describe("myApp.sectionPool module", function() {

	  beforeEach(module("myApp.sectionPool"));

	  describe("sectionPool controller", function(){

	    it("should ....", inject(function($controller) {
	      //spec body
	      var sectionPoolCtrl = $controller("SectionPoolCtrl");
	      expect(sectionPoolCtrl).toBeDefined();
	    }));

	  });
	});
})(angular);